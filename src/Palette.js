import React from 'react';

const Palette = props => {
  return (
    <div className="Palette">
      {/* navbar */}
      <div className="Palette-colors">{/* other colors */}</div>
      {/* footer */}
    </div>
  );
};

export default Palette;
